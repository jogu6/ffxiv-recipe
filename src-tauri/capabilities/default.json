{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the pipeline GUI.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
